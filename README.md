# JonasTodo

This is a private application created to practice and showcase my knowledge, it is not (yet) intended for public use. It uses a simple terminal user interface to interact with a database (MS SQL Server) through Entity Framework. Connection string is managed offline (Azure keystore).
